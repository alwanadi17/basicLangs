Package

package is definitely used for grouping related classe. That's why it called
package. When we want to run a program inside a package, run with
java <package name>.<program name>

//run main
java main

//run myProgram
java myProgram.myProgram
